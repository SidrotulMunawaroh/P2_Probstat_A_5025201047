#2a. Apakah Anda setuju dengan klaim tersebut? Setujuuu
#2b. Jelaskan maksud dari output yang dihasilkan!
library(BSDA)

   tsum.test(
   mean.x = 23500, 
   s.x = sd(3900), 
   n.x = 100
   )
   
#2c. Buatlah kesimpulan berdasarkan P-Value yang dihasilkan!
kesimpulan dari soal diatas adalah mobil yang digunakan rata ratanya lebih dari 20.000 km/tahun
